name: ios-ci
on: [push, pull_request]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun test scripts/generate-ios-strings.test.ts
      - run: bun scripts/generate-ios-strings.ts
      - run: command -v xcodegen || brew install xcodegen
      - run: cd apps/ios && xcodegen generate
      - name: Swift Testing unit tests (ZenAPIClient)
        run: cd apps/ios/ZenAPIClient && swift test
      - name: Build for the simulator
        run: |
          cd apps/ios
          # No explicit -sdk: it would force every embedded target (including the watchOS
          # complication extension) onto the iphonesimulator SDK. -destination alone lets each
          # target build against its own platform.
          xcodebuild build \
            -project Zen.xcodeproj -scheme Zen \
            -destination "platform=iOS Simulator,name=iPhone 17" \
            -skipPackagePluginValidation \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
      - name: Install the app on a booted simulator
        run: |
          set -euo pipefail
          UDID=$(xcrun simctl list devices available -j | python3 -c '
          import json, sys
          devices = json.load(sys.stdin)["devices"]
          for runtime, ds in devices.items():
              for d in ds:
                  if d["name"] == "iPhone 17":
                      print(d["udid"]); sys.exit(0)
          sys.exit(1)')
          xcrun simctl boot "$UDID" || true
          # -derivedDataPath above pins the build output under apps/ios/DerivedData — the
          # default (~/Library/Developer/Xcode/DerivedData/<hash>) isn't reliably findable.
          APP_PATH=$(find apps/ios/DerivedData/Build/Products -maxdepth 2 -name "Zen.app" | head -1)
          test -n "$APP_PATH"
          xcrun simctl install "$UDID" "$APP_PATH"
          echo "SIMULATOR_UDID=$UDID" >> "$GITHUB_ENV"
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - name: Install Maestro
        run: curl -Ls "https://get.maestro.mobile.dev" | bash
      - name: Run the shared Maestro smoke flow
        run: |
          export PATH="$PATH:$HOME/.maestro/bin"
          maestro --device "$SIMULATOR_UDID" test -e APP_ID=com.dinh144.zen flows/smoke.yaml
